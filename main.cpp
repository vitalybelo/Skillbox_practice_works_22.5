#include <iostream>
#include <string>
#include <map>
#include "windows.h"
using namespace std;


int main() {
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    setlocale(LC_ALL, "RUS");

    string comm;
    map<int, string> registryMap;

    do {



    } while (comm == "quit");

    return 0;
}
